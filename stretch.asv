% Function that normalizes a vector between the specified range.% INPUT:%   · image: Vector to normalize.%   · varargin: 1x2 cell of the normalization range.% OUTPUT:%   · renormalized: Normalized vector.%   · out: Minimum and maximum of ?image? vector.function [renormalized, out] = stretch(image, varargin)if (nargin > 3)	error('Wrong number of arguments.');endminValue = 0;range = 1;if (nargin == 3)	minValue = varargin{1:1};	range = abs(varargin{2:2} - varargin{1:1});endim = double(image);renormalized = range * (im - min(min(im))) ./ (max(max(im)) - min(min(im))) + minValue;out = [min(min(im)) max(max(im))];