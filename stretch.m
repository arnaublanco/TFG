% Function that normalizes an input array.%  INPUT:%   · image: Input array.%   · varargin: Minimum and maximum (if specified)%  OUTPUT:%   · renormalized: Normalized input array.%   · out: Maximum and minimum values.function [renormalized,out] = stretch(image, varargin)if (nargin > 3)	error('Wrong number of arguments.');endminValue = 0;range = 1;if (nargin == 3)	minValue = varargin{1:1};	range = abs(varargin{2:2} - varargin{1:1});endim = double(image);renormalized = range * (im - min(min(im))) ./ (max(max(im)) - min(min(im))) + minValue;out = [min(min(im)) max(max(im))];