% Function that normalizes a vector into a given scale by specifying the% minimum and maximum possible values (pars).%  INPUT:%   · im: Vector to normalize.%   · scale: Scale to which normalize.%   · pars: Minimum and maximum possible values.%  OUTPUT:%   · renormalized: Normalized vector.function [renormalized] = stretchWithGivenPars(im, scale, pars)minValue = 0;range = 1;if (exist('scale', 'var'))	minValue = scale(1);	range = abs(scale(2) - scale(1));endim = double(im); [a, b] = size(im);if(b == 1)    if(size(pars) == [1 2])        renormalized = range * (im - pars(1,1)) ./ (pars(1,2)-pars(1,1)) + minValue;    else        error('Parameters do not fit time series');    endelse    renormalized = zeros(a,b);    for i = 1:b        renormalized(:,i) = range * (im(:,i) - pars(i,1)) ./ (pars(i,2)-pars(i,1)) + minValue;    endend